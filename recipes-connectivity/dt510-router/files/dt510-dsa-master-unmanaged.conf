# DT510 — KSZ9896 DSA: end0 is the CPU/master netdev (leave unmanaged).
# WAN (lan1): DHCP client — profile DT510-wan.
# LAN (lan2–lan4): bridge br-lan — DT510-lan-bridge uses ipv4.method shared (DHCP server to LAN + NAT out WAN).
[device]
match-device=interface-name:end0
managed=false
